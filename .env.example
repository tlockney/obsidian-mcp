# Obsidian Local REST API Configuration

# Required: URL where Obsidian Local REST API is running
# Default port is 27123, change if you've configured it differently
OBSIDIAN_API_URL=http://localhost:27123

# Optional but recommended: Your API key from the Obsidian plugin
# Get this from: Obsidian Settings → Community Plugins → Local REST API → Settings
# Leave empty or comment out if no API key is configured in the plugin
OBSIDIAN_API_KEY=your-api-key-here